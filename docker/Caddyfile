{
	frankenphp
	auto_https off
	admin off

	log {
		output stderr
		format json
	}
}

:8080 {
	root * /app/public

	# php_server handles the front-controller pattern for us: it try_files
	# down to index.php, which is the only entry point this device has.
	php_server

	encode zstd gzip
}
